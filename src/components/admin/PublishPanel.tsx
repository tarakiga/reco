"use client";
// PLACEHOLDER — replaced by the real publish/version/rollback panel in Plan 2b Task 5.
export function PublishPanel(_props: {
  entityType: "options_namespace" | "content_block";
  entityKey: string;
  canRollback: boolean;
}) {
  return null;
}
